# Seed Templates — scaffold_2

Minimal starter files per branch type. No project-name substitution. Placeholder only.

## Key Templates

main: `# <project-name>` + worktree list hint.

Planning: README + (02_context) CONTEXT.md with interview Q&A structure.

Spec: standard spec format — behavior, branding, UX copy, anti-patterns.

Testing: spec-driven TDD note.

Impl tech-stack: boilerplate confirmation note.

App-shell: merge target note.

Impl feature: fork of app-shell, specs reference.

Staging: fork of app-shell, receives impl merges.

Deployment: merged from staging + bug-fixes.